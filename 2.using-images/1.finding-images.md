To find images from the main Docker hub, we use the command `docker search [imagename]`

```bash
yosang@localhost:~$ docker search fedora
NAME                     DESCRIPTION                                     STARS     OFFICIAL
fedora                   Official Docker builds of Fedora                1253      [OK]
```